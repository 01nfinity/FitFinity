# Secrets (copy .env.example to .env and fill in real values)
.env

# OS
.DS_Store

# Local git recovery backup (see conversation history for context)
.git.corrupted-backup-*/

# Dependencies (each subproject also has its own .gitignore)
node_modules/
